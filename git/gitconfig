[color]
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    ui = true   

[user]
    name = Francis Chartrand
    email = chartrandf@bonyweb.com

[alias]
    unstage = reset HEAD -- 
    rollback = reset --soft HEAD~1
    st = status
    cm = commit
    br = branch
    co = checkout
    df = diff
    oush = push
    gl = log --graph --abbrev-commit --pretty=oneline --decorate
    graphlog = gl
    branchr = branch --sort=-committerdate --format=\"%(committerdate:relative)%09%(refname:short)\"

[core]
    ignorecase = false
